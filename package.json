{
  "name": "ai-copilot-parents",
  "version": "1.0.0",
  "description": "AI Copilot for Parents - Full Stack Application",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "start": "npm start --prefix server",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
